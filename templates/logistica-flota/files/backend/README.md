# TPLLOGISTICA Backend

This is the backend of the TPLLOGISTICA project, built with FastAPI.

## Development

Run `pip install -r requirements.txt` to install dependencies and `uvicorn main:app --reload` to start the development server.