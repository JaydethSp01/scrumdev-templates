# TPLLOGISTICA Frontend

This is the frontend of the TPLLOGISTICA project, built with Next.js and Tailwind CSS.

## Development

Run `npm install` to install dependencies and `npm run dev` to start the development server.